import React from 'react';
import AppRem from './components/AppRem';
//import AuthContext from "./context/auth"

export default function Remits() {
  //  const { user } = useContext(AuthContext)

  return (
    <>
      <AppRem />
    </>
  );
}
