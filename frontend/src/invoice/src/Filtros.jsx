import React from 'react';
import AppFiltro from './components/AppFiltro';
//import AuthContext from "./context/auth"

export default function Filtros() {
  //  const { user } = useContext(AuthContext)

  return (
    <>
      <AppFiltro />
    </>
  );
}
